// Widget smoke tests are skipped because the app requires async
// initialization (Supabase, FlutterSecureStorage) that cannot be
// trivially bootstrapped in a widget test environment.
// See test/unit/ for the calculation-logic unit tests.

void main() {
  // Placeholder — unit tests live in test/unit/
}
